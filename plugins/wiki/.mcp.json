{
  "mcpServers": {
    "wiki": {
      "command": "uv",
      "args": [
        "tool",
        "run",
        "--quiet",
        "--from",
        "git+https://github.com/wddawson/wiki-agent.git@main#subdirectory=server",
        "wiki-mcp"
      ],
      "env": {
        "GITLAB_TOKEN": "${GITLAB_TOKEN}",
        "GITLAB_PROJECT": "wddawson/l_sim-agent-wiki"
      }
    }
  }
}
